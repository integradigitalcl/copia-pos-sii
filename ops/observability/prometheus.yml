global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "grunflexpos-api"
    metrics_path: /metrics
    scheme: https
    static_configs:
      - targets: ["host.docker.internal:7279"]
    tls_config:
      insecure_skip_verify: true
