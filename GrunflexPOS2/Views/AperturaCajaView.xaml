<Window x:Class="GrunflexPOS2.Views.AperturaCajaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        Height="400"
        Width="420"
        AllowsTransparency="True">

    <Border Background="#252526"
            CornerRadius="12"
            Padding="25">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="40"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Apertura de Caja"
               Foreground="White"
               FontSize="20"
               FontWeight="Bold"
               VerticalAlignment="Center"/>

                <!-- BOTÓN CERRAR ROJO -->
                <Border Grid.Column="1"
            Background="#C42B1C"
            CornerRadius="0,12,0,0">

                    <Button Content="✕"
                Background="Transparent"
                Foreground="White"
                BorderThickness="0"
                Cursor="Hand"
                FontSize="14"
                FontWeight="Bold"
                Click="Cerrar_Click">

                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#E81123"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Border>
            </Grid>

            <!-- FORM -->
            <StackPanel Grid.Row="1" Margin="0,20,0,0">

                <TextBlock Text="Número de Caja" Foreground="White"/>
                <TextBox x:Name="NumeroCajaTextBox"
                         Height="35"
                         Margin="0,5,0,15"
                         Background="#333337"
                         Foreground="White"
                         BorderThickness="0"
                         Padding="8"/>

                <TextBlock Text="Nombre del Cajero" Foreground="White"/>
                <TextBox x:Name="CajeroTextBox"
                         Height="35"
                         Margin="0,5,0,15"
                         Background="#333337"
                         Foreground="White"
                         BorderThickness="0"
                         Padding="8"/>

                <TextBlock Text="Monto Inicial ($)" Foreground="White"/>
                <TextBox x:Name="MontoInicialTextBox"
                         Height="35"
                         Margin="0,5,0,25"
                         Background="#333337"
                         Foreground="White"
                         BorderThickness="0"
                         Padding="8"
                         PreviewTextInput="MontoInicial_PreviewTextInput"
                         TextChanged="MontoInicial_TextChanged"/>

                <Button Content="ABRIR CAJA"
                        Height="40"
                        FontWeight="Bold"
                        Background="#007ACC"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="AbrirCaja_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>