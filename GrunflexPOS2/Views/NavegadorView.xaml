<UserControl x:Class="GrunflexPOS2.Views.NavegadorView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">

    <Grid>

        <!-- Barra superior -->
        <StackPanel Orientation="Horizontal" Background="#F5F5F5" Height="40">

            <Button Content="◀" Width="40" Click="Back_Click"/>
            <Button Content="▶" Width="40" Click="Forward_Click"/>
            <Button Content="⟳" Width="40" Click="Reload_Click"/>

            <TextBox x:Name="TxtUrl"
                     Width="500"
                     Margin="10,5"
                     VerticalAlignment="Center"/>

            <Button Content="Ir" Width="50" Click="Go_Click"/>

        </StackPanel>

        <!-- Navegador -->
        <wv2:WebView2 x:Name="Browser"
                      Margin="0,40,0,0"/>

    </Grid>
</UserControl>